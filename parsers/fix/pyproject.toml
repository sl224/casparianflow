[project]
name = "fix_parser"
version = "1.2.0"
description = "FIX Protocol Parser for Casparian Flow"
requires-python = ">=3.9"
dependencies = [
    "pyarrow>=15.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
