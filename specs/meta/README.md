# Meta Specs (Internal)

This directory contains internal workflow docs, audit artifacts, and
spec-maintenance sessions. These files are not product specs and are not
kept in sync with the current codebase.

Canonical product specs live in `docs/specs_canonical.md`.
